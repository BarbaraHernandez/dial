import React from 'react'

function TeamBlock({userId}) {
    return (
        <div>
            <p>this block is for user {userId}</p>
            <p>they have  X hours : y vacay : z sick</p>
        </div>
    )
}

export default TeamBlock
